import styled from "styled-components";
import {palette} from "../../constants/Styles";

export const Date = styled.input`
      font: inherit;
      border-radius: 8px;
      border: 2px solid ${palette["--color-gray-300"]};
    `;