import styled from "styled-components";
import {palette} from "../../constants/Styles";

export const Line = styled.span`
      width: 1px;
      height: 50px;
      background-color: ${palette["--color-gray-900"]};
    `;